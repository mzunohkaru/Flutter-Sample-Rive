name: rive_sample
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  rive: ^0.13.12

  flutter_gen: ^5.7.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.9

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/
